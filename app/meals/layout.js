import React from 'react'

const Layout = ({children}) => {
    //this layout only intended for the meals page. 
  return (
    <div>Meals Layout
    {children}
    </div>
  )
}

export default Layout